"use client";

/**
 * QuestionStage —— 问题输入阶段。
 *
 * 当前 UI 的问题输入在 / 首页完成（HeroEntry），进入 /reading 时 question 已由 URL
 * 携带。这里保留一个占位，方便未来在解读中途追问/修改问题时直接渲染。
 */
export default function QuestionStage(): null {
  // TODO(reading-core-stabilization): 当解读流程支持中途改写问题时填充。
  return null;
}
